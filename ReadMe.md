### Requirements

Execute:
* SFML

Compile:
* Make
* CMakeLists
* C++ Compiler

GitHub: [Orbit-M](https://github.com/CMBowler/Orbit-M)

### Compile:

`mkdir build && cd build`

`cmake ..`

Build: `cmake . && make`

Clean: `cmake --build . --target clean`
